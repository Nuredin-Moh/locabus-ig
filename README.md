# Locabus — publication social quotidienne (Instagram + Facebook)
Auto-publie 1 carrousel/jour sur IG et FB via GitHub Actions (`publish_daily.py`), independant de tout ordinateur.
Planning dans `schedule.json`. Anti-doublon dans `last.json`. Secrets : IG_TOKEN, IG_USER_ID, FB_PAGE_ID.
